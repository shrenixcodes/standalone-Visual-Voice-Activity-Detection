# Configuration

Use `config.example.yaml` as a starting point and load it with `visual_vad.config.load_config`. Thresholds, filter strategy, debounce counts, queue capacity, camera, detector, tracker, mesh, and ROI settings are typed configuration values.
