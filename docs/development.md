# Development

Create a virtual environment, install with `pip install -e .`, and run `python -m unittest discover -s tests`. Keep code formatted with Black and linted with Ruff. Hardware validation requires a webcam.
